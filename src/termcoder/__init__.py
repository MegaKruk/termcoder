"""termcoder: a privacy-first, terminal-native agentic coding assistant.

This package is intentionally split into small, single-responsibility modules so
that later phases (sandboxed execution, context compaction, repo mapping, web
search, MCP, sub-agents) can be added without reworking the core agent loop.
"""

__version__ = "0.1.0"
